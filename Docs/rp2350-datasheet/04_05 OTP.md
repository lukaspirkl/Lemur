# 4.5 OTP

RP2350 contains 8 kB of one-time-programmable storage (OTP), which stores:

- Manufacturing information such as unique device ID
- Boot configuration such as non-default crystal oscillator frequency
- Public key fingerprint(s) for boot signature enforcement
- Symmetric keys for decryption of external flash contents into SRAM
- User-defined contents, including bootable program images [\(Section 5.10.7\)](#page-437-0)

The OTP storage is structured as 4096 × 24-bit rows. Each row contains 16 bits of data and 8 bits of parity information, providing 8 kB of data storage. OTP bit cells are initially 0 and can be programmed to 1. However, they cannot be cleared back to 0 under any circumstance. This ensures that security-critical flags, such as debug disables, are physically impossible to clear once set. However, you must also take care to program the correct values.

For more information about the OTP subsystem, see [Chapter 13](#page-1265-0).

4.5. OTP **352**

# <span id="page-353-0"></span>**Chapter 5. Bootrom**

Each RP2350 device contains 32 kB of mask ROM: a physically immutable memory resource described in [Section 4.1](#page-337-1). The RP2350 bootrom is the binary image etched into this ROM that contains the first instructions executed at reset.

The bootrom concepts section ([Section 5.1](#page-354-0)) covers the following topics, which are necessary background for understanding the bootrom features and their implementation:

- [Partition tables](#page-354-2) and their associated [flash permissions](#page-355-0)
- [Bootable images,](#page-355-1) and the [block loops](#page-356-1) that store their metadata
- [Versioning](#page-357-0) for images and partition tables, and [A/B versions](#page-357-1) to support double-buffered upgrades
- [Hashing and signing](#page-357-2) to support secure boot with public key fingerprint in OTP (see also [Section 10.1.1](#page-813-2) in the security chapter)
- [Load maps](#page-358-0) for bootable images, and [packaged binaries](#page-358-1) which the bootrom loads from flash into RAM according to the image's load map
- [Anti-rollback protection](#page-359-0) to revoke older, compromised versions of software
- Three forms of flash boot:
  - [Flash image boot](#page-359-1), with a single binary image written directly into flash
  - [Flash partition boot,](#page-360-0) with the boot image selected from the partition table
  - [Partition-table-in-image boot,](#page-360-1) where the boot image is not contained in a partition table, but still embeds a partition table data structure to divide the flash address space
- [Boot slots](#page-360-2) for A/B versions of partition tables
- [Flash update boot,](#page-361-0) a special one-time boot mode that enables version downgrades following an image download
- [Try before you buy](#page-362-0) support for phased upgrades with image self-test
- [Address translation](#page-363-0) for flash images, which provides a consistent runtime address to images regardless of physical storage location
- [Automatic architecture switch](#page-364-0) when attempting to run a RISC-V binary on Arm, or vice versa
- [Targeting UF2 downloads](#page-362-1) to different flash partitions based on their permissions and the UF2 family ID

Besides features mentioned as concepts above, the RP2350 bootrom implements:

- The core 0 initial boot sequence ([Section 5.2\)](#page-364-1)
- The core 1 low-power wait and launch protocol [\(Section 5.3\)](#page-375-1)
- Runtime APIs ([Section 5.4](#page-376-0)) exported through the ROM symbol table, such as flash and OTP programming
- A subset of runtime APIs available to Non-secure code, with permission for each API entry point individually configured by Secure code
- A USB MSC class-compliant bootloader with [UF2](https://github.com/Microsoft/uf2) support for downloading code/data to flash or RAM [\(Section 5.5\)](#page-398-0), including support for versioning and A/B partitions
- The USB PICOBOOT interface for advanced operations like OTP programming ([Section 5.6\)](#page-402-0) and to support picotool or other host side tools
- Support for white-labelling all USB exposed information/identifiers ([Section 5.7\)](#page-410-0)
- A UART bootloader providing a minimal shell to load an SRAM binary from a host microcontroller ([Section 5.8](#page-414-0))

You should read the bootrom concepts section before diving into the features in the list above. RP2350 adds a considerable amount of new functionality compared to the RP2040 bootrom. If you are in a terrible hurry, [Section 5.9.5](#page-426-0) covers the absolute minimum requirements for a binary to be bootable on RP2350 when secure boot is not enabled.

Chapter 5. Bootrom **353**

#### **Bootrom Source Code**

All source files for the RP2350 bootrom are available under the terms of the 3-clause BSD licence:

[github.com/raspberrypi/pico-bootrom-rp2350](https://github.com/raspberrypi/pico-bootrom-rp2350)

