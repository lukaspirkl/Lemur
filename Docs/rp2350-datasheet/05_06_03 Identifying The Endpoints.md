# 5.6.3 Identifying The Endpoints

The PICOBOOT interface provides a single BULK\_OUT and a single BULK\_IN endpoint. These can be identified by their direction and type. You should not rely on endpoint numbers.

